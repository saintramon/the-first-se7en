function revealLetter() {
   //const [letter, setLetter] = useState(null);
   // const [err, setErr] = useState(null);
    console.log("test");
    /*
    useEffect(() => {
      console.log("Reached")
      
      fetch('http://localhost:8080/api/quest/revealLetter')
      .then((res) => res.json())
      .then(data => {
        console.log("Letter revealed", data);
        if (data?.error) {
          setErr(data.error);
          return err;
        } else {
          setLetter(data);
        }
    });
    return letter;
  });
  */
}
export default revealLetter;

